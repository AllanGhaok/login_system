<IfModule mod_rewrite.c>
	RewriteEngine on
	RewriteRule ^$ public/ [L]
	RewriteRule (.*) public/$1 [L]
</IfModule>

#Como deu problema na pasta eu vou deixar comentado por enquanto até o professor me responder como resolver...